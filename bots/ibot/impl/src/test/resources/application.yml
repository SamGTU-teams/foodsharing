spring:
  cloud:
    config:
      enabled: false
datasource:
  driverClassName: org.h2.Driver
  url: jdbc:h2:mem:testDb;CLOSE_ON_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
  username: sa
  password: sa
