server:
  port: 8030
spring:
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  application:
    name: tg-bot
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config:8888
