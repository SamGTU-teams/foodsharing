spring:
  application:
    name: analyzer
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888/}
  profiles:
    active: ${spring_profiles_active:default}

server:
  port: ${SERVER_PORT:8010}

management:
  endpoints:
    web:
      exposure:
        include: health,info

