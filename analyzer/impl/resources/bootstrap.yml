server:
  port: 8010
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: analyzer
---
spring:
  config:
    activate:
      on-profile: docker
  cloud:
    config:
      uri: http://config:8888
