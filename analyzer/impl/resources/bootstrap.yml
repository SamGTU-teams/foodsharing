spring:
  application:
    name: analyzer
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888/}
  profiles:
    active: ${SPR_PROFILE:default}

server:
  port: ${SERVER_PORT:8010}

