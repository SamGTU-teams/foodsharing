version: "3"
services:
  rabbitmq:
    image: rabbitmq:3-alpine

  postgres:
    image: postgres:10-alpine

  config:
    build:
      context: ./config-service
      dockerfile: Dockerfile

  parser:
    build:
      context: ./parser
      dockerfile: Dockerfile
    restart: "no"

  analyzer:
    build:
      context: ./analyzer
      dockerfile: Dockerfile
    restart: "no"

  ibot:
    build:
      context: ./ibot
      dockerfile: Dockerfile

  tg-bot:
    build:
      context: ./tg-bot
      dockerfile: Dockerfile

  vk-bot:
    build:
      context: ./vk-bot
      dockerfile: Dockerfile
