spring:
  rabbitmq:
    exchangeName: post-raw-exchange
  liquibase:
    change-log: classpath:/db/changelog/master-changelog.xml
    contexts: vk-parser,data,test

vk:
  parser:
    callbackUrl: localhost:${server.port:8080}/callback
    serverTitle: GetFood_Parser
